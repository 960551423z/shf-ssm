<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.zq.dao.HouseBrokerDao">

    <sql id="columns">
        select id,house_id,broker_id,broker_name,broker_head_url,create_time,update_time,is_deleted
        from mydb.hse_house_broker
    </sql>

    <!--查询经纪人信息    -->
    <select id="getHouseBrokerByHouseId" resultType="houseBroker">
        <include refid="columns"/>
        where house_id = #{houseId} and is_deleted = 0
    </select>

    <!--添加经纪人信息    -->
    <insert id="insert" parameterType="houseBroker">
        insert into mydb.hse_house_broker(house_id, broker_id, broker_name, broker_head_url)
        values(#{houseId},#{brokerId},#{brokerName},#{brokerHeadUrl})
    </insert>

    <!--查询单个经纪人    -->
    <select id="getById" resultType="houseBroker">
        <include refid="columns"/>
        where id = #{id}
    </select>
    
    <!-- 修改更新   -->
    <update id="update" parameterType="houseBroker">
        update mydb.hse_house_broker
        <set>
            <if test="brokerId != null and brokerId !=''">
                broker_id = #{brokerId},
            </if>
            <if test="brokerName != null and brokerName !=''">
                broker_name = #{brokerName},
            </if>
            <if test="brokerHeadUrl != null and brokerHeadUrl !=''">
                broker_Head_Url = #{brokerHeadUrl},
            </if>
        </set>
            where id = #{id}
    </update>

    <update id="delete">
        update mydb.hse_house_broker
        set is_deleted = 1
        where id = #{id}
    </update>
</mapper>